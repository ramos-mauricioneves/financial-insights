[phases.setup]
nixPkgs = ["python310Full", "nodejs", "python310Packages.pip"]

[phases.install]
cmds = ["python install_deps.py"]

[phases.build]
cmds = ["echo 'Build já foi feito no install'"]

[start]
cmd = "bash run.sh" 